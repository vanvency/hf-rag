version: "3.9"

services:
  rag-api:
    build: .
    command: uvicorn src.main:app --host 0.0.0.0 --port ${API_PORT:-8000}
    ports:
      - "${API_PORT:-8000}:8000"
    volumes:
      - ./:/app
    env_file:
      - .env
    restart: unless-stopped

